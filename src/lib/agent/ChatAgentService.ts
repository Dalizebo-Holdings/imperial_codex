/**
 * ChatAgentService — builds the tool set for the chat API route.
 *
 * Exports buildTools() which returns all 8 AI tool definitions
 * bound to the current in-memory store and Supabase clients.
 */

export { buildTools } from './tools';
